# grp6 RTDI system

Updated September 20, 2026. This is the single authority for status, architecture requirements, contracts, runbook and acceptance. [AGENTS.md](AGENTS.md) owns assistant instructions; [team/](team/) contains assignments only.
Machine-running code and correlated evidence are golden. Local source defines local behavior; newer source does not prove deployment. Historical packages/captures remain revision-specific evidence.
Current acceptance is the complete Gemini host-relay run below; older run/package/deployment observations retain their original limits.
September 20 restart recovery: the initial read-only inspection found the relay and SmarTest stopped with retained evidence and ledger intact. The user then explicitly authorized restoring tester/relay and running a device batch. Existing workspace and stopped ledger were backed up under `/home/user/grp6-recovery-20260920-Qf90Tu`; SmarTest, saved host relay configuration and the pinned Edge application were restored without a VM reboot, Nexus restart or model change. Startup scope `4620bc260aef42329930bbf46d35b13f` delivered tp_info. The supplied four-site device execution created a separate source run `74c4e8ccd7f446d3bfcdf2ab7b668f6d`: public backend has 99 events, 24 predictions with 24 actuals, four completed devices, supplied yield 100%, and zero incidents. Browser confirmed connected SSE and the new run in the picker. These are supplied ORE results, not physical independent validation. See [final audit](results/continuation_20260920/final_read_only_audit.md).

Frontend recovery follow-up: the shared pickers omit the two user-retired recovery probes (4620bc / 74c4), while retaining their backend evidence. New group-6 host-relay scopes are named Gemini run. Local validation passes 269 frontend tests, TypeScript and production build. The user-requested full supplied 80-device recipe is running as a1b8a0fab85241c2b8babd2f1062af23; incremental public delivery was observed at 607 events / 172 predictions before completion. Only supplied wafer 02 is verified for this tester input; the 25-wafer summary remains a historical replay, not a multi-wafer live test.

## Status and evidence

| Scope | Result | Evidence / limit |
| --- | --- | --- |
| Current runtime deployment | TCCT message-fragment fix and hardened exporter built/pushed as `unifiedserver.local/grp6/py-app:20260919T175744779996Z`; 56 packaged container tests and real SDK Monitor construction passed. Both descriptors pinned; AppDeployer stop/start exited 0; fresh Monitor observed. Export remains disabled. | [Deployment and receiver observation](results/continuation_20260920/transport_deployment_observation.json). Registry digest verified; actual Edge digest remains unverified. The earlier 084518Z release is historical. |
| Engineering history | Six stages × four sites, full selected-feature coverage, 24 actuals, six successful tester actions; callback latency 0.419–1.741 ms. | [Engineering evidence](results/vm_engineering/); delayed-data recovery and accuracy tolerance remain open. |
| Production run 3 | 80 devices, 74 pass / 6 fail; 120 requests, 480/480 actual joins; continuous sequence 1–765. Strict action validation fails on sequence 319. | [Strict audit](results/vm_production/strict_action_audit_20260919.json) supersedes the old callback pass; counts/integrity remain intact. [Report](results/vm_production/grp6_core_prod3_report.html); max callback latency 8.350 ms. |
| Prediction / anomaly receipts | 120/120 prediction action matches with successful adjacent execution. All three device-32 anomaly IDs reached TCCT On_POSTBIN output. | [Prediction audit](results/vm_production/tester_receipt_audit.json), [receiver observation](results/vm_production/receiver_observation_20260919.json). Anomaly parsing/execution/display remain unverified. |
| Core verification | 75 local tests pass, including 27 monitor tests and 22 exporter ACK/outage/restart/quarantine tests; 12 release-preparation tests also pass. The scoped TCCT workaround and hardened exporter are now packaged/deployed; live export remains off. | [Receiver analysis and tests](results/continuation_20260920/message_analysis.md), [release safeguards](results/continuation_20260920/exporter_preparation.md), [separate deployment observation](results/continuation_20260920/transport_deployment_observation.json). Local and package tests do not prove live transport. |
| Current Gemini host relay | Fresh run `04dcb07358ee4f3da41e5cbc12cb9850`: 80 devices, 74 pass / 6 fail, 120 requests, 480 strict actual joins, 20 valid production replies, all three anomaly Insert receipts. Continuous sequence 1–764. | [Correlated acceptance](results/continuation_20260920/fresh_relay_acceptance.json). Real Gemini processing of supplied ORE input, not independent physical validation. Native exporter disabled; host EdgeLog relay active. |
| Earlier transport acceptance run | 80 devices, 119 requests / 476 actuals, three successful anomaly Insert receipts. | [Earlier observation](results/continuation_20260920/transport_deployment_observation.json). Touchdown-9 stage-1 request is absent; root cause remains unresolved. Latest complete run does not establish that the gap cannot recur. |
| Fresh supplied-program evaluation | Complete run `b7deb9e5ff1641c6a16546eda090c121`: 80 devices, 74 pass / 6 fail; 480/480 actual joins; 120/120 tester prediction executions; 20/20 valid production JSON replies. Overall MAE 0.216342. | [Fresh evaluation and dataflow audit](results/dataflow_evaluation_20260920/REPORT.md). Three anomaly IDs reach TCCT, but ProdMessage rejects their combined text. At that historical check the website remained replay-only; subsequent host-relay acceptance is recorded above. Repeated ORE input is not new independent data. |
| Current dashboard/backend | Minimal Workspace metrics/timestamps; Analysis defaults to the restored 25-wafer summary.json archive; a single Replay/Live picker also opens stored-run wafer overviews with snapshot/SSE and retained Yield & measurement coverage; four bilingual Sandbox examples; Analysis → Workspace → Sandbox guide. | Archive/grouped-picker local validation: 245 frontend tests, TypeScript and production build pass. Browser confirms archive/backend switching and retained coverage; the bilingual Guide adapts to the selected source. Earlier release validation remains in demo_release_verification.json; deployment is identified by VPS current/REVISION. |
| VPS development preview | Prior verified public release served the minimal Workspace, scoped snapshot/SSE Analysis, separate offline archive, four bilingual Sandbox exercises and Workspace → Analysis → Sandbox guide. Historical application implementation `de8ff5c776d49fd032e4398d1c23353fef73c451` was activated and publicly verified. The later wafer-overview release removed the archive and reversed the first two navigation/guide positions; the current source restores the archive with one Replay/Live picker. current/REVISION identifies the active publication. | [Public release verification](results/continuation_20260920/demo_release_verification.json): four routes and 18 assets pass; five stored scopes, including two newly imported authentic captures (814 accepted; repeat 814 duplicates). Fresh host scope: 1,723 events, 480 predictions/actuals, three incidents/evidence, 92.50% final supplied yield; complete SSE replay and heartbeat pass. Commands disabled; zero paid model calls. VPS current/REVISION records the activated publication SHA. |
| Integrated backend/frontend | Browser-tab sources, imports, drafts and scoped navigation persist; live host records and authentic historical captures retain distinct provenance. Public Workspace/Analysis navigation, original timestamps, repaired yield, guide order, English/Chinese UI and missing/late Sandbox example verified. | [Release checks](results/continuation_20260920/demo_release_verification.json). Full frontend suite 238 passed at application revision de8ff5c; subsequent helper symlink/import-only regression suite 21 passed, zero skips. Hosted D1 concurrency, public authentication, durable quotas, complete native measurements and direct-container export remain open. Earlier UI acceptance records are historical. |
| B/C offline proposals | B: 13 tests and saved evidence audit pass; sparse fallback improves synthetic missing-input availability but standalone accuracy regresses in five stages. C: 11 tests and evidence audit pass; 6/7 coverage unchanged, W25 missed, two extra alerts. | [B evaluation](workstreams/prediction/EVALUATION.md); [C proposal](workstreams/detection/PROPOSAL.md). Keep primary models; reject C promotion. No runtime/artifact change. |
| R2 offline review | B readiness/receipt audit reproduces engineering 6/24 and production 120/480 request/actual joins; 18 production waits, all stage 1. C's site-key optimization is deferred: median paired wafer saving 0.86%, only 59/100 pairs faster. | [B machine matrix](workstreams/prediction/ROUND2.md), [C profile](workstreams/detection/ROUND2.md), [A evidence review](results/r2_evidence_review_20260919.json). B/C have 36/20 passing tests. No runtime/model promotion. |
| R3 W25 research review | The candidate adds W25 spread-down at device 80 and preserves all 14 baseline alerts, but fails 20/20 constant-variance mean-step controls plus B's impulse control. **Reject runtime and replay promotion.** | [A reproducible review](results/r3_research_review.json): 1,225 B/C scan statistics agree; A independently reproduced false alerts. 48 targeted research tests pass, including characterization of failures; this is not detector acceptance. Runtime remains 6/7 with W25 missed. |
| R3 sparse-burst follow-up | The supplied hypothesis reproduces W25 devices 5, 6, 17, 18, 19, 35, 38, 78: seven early versus one late. Corrected scans miss W25; relaxed scans add a false alert on normal-labeled W15. **Accept the diagnostic, reject runtime/replay promotion.** | [A cross-check](results/burst_followup_review.json): 1,050 independent B/C scan matches and all 14 baseline payloads preserved. [B report](workstreams/prediction/followup_burst_20260919/REPORT.md), [C report](workstreams/detection/followup_sparse_burst/REPORT.md). All 25 wafers are reused development data; reduced burst incidence does not establish reduced noise variance. |
| Offline analysis | 25 CSVs, 2,000 devices, 3,036 measurements; six Ridge models, five wafer-separated folds; 12,000 replay predictions, expected category on 6/7 labeled anomaly wafers. | [Validation](grp6_app/artifacts/validation.json), [wafer/site validation](results/model_revalidation/validation.json), [replay](results/replay/summary.json). W25 spread decrease missed. |
| User-authorized W25 source candidate | Source commit `a869f185e37a6b1547cedf0fb0c6cab896818c24` is merged into local main. The new sparse-burst supplement is active when its separate artifact is present: W25 spread-down@72, W15 no alert, zero new normal-label alerts, all 14 core payloads unchanged; 70 core tests pass. | [Integration rerun](workstreams/detection/sparse_burst_w25/integration-results.json), [candidate report](workstreams/detection/sparse_burst_w25/REPORT.md). **Development candidate, not independent validation or deployment acceptance.** Dedicated detector unit tests, synthetic controls and adversarial review remain open. Accepted `results/replay` and live/VPS deployment are unchanged (W25 still missed there). |

The earlier browser SSH evaluation confirmed `group-6` and ran the supplied ORE recipe twice without restarting Nexus or recreating the workspace. The complete second run has continuous sequence 1–764, the same frozen model `752e…`, 480 strict joins and 120 confirmed prediction executions. Machine was left bound/idle, SMT8 ready with Edge available; bounded collectors ended. Raw evidence remains in `/home/user/Case_Event/dataflow_evaluation_20260920_Ns5HDP`; [fresh report](results/dataflow_evaluation_20260920/REPORT.md) distinguishes browser-transcribed observations from locally held historical captures.
Historical sequence-319 get_prod has a literal LF in JSON. The earlier fresh evaluation passed strict parsing for all 20 production replies, but TCCT forwarded combined anomaly text that ProdMessage rejected with `Warn: does not support`. September 20 receiver inspection proved two literal LFs in raw FIFO JSON; a direct-FIFO diagnostic verified parsing/display only. The subsequently deployed workaround encodes only production acs_prod_var/msg reason fragments once before TCCT interpolation. Fresh lot `3f46468325ac47de894e6a13d3c672e0` now confirms the actual runtime/TCCT receiver path: sequence 318 at 18:09:01 UTC matches receiver local 02:09:01, with IDs `46468155985a`, `cc42f5324282`, `f370ac476ab4` in successful Insert text lines 610–612. [Deployment observation](results/continuation_20260920/transport_deployment_observation.json) preserves remote paths/hashes and distinguishes browser transcription from raw download. This run has a separate touchdown-9 stage-1 gap (119/120 requests, 476/480 actual joins), despite continuous events and no recorded callback errors. Final poll 758 precedes boundary 759 by two seconds; final-boundary delivery and actual Edge digest remain open. [Historical analysis](results/continuation_20260920/message_analysis.md) remains unchanged.

The user deferred W2/W25 investigation on September 20 because PDF labels may not be golden; W2 raw yield remains 53.75%. Do not promote the sparse-burst candidate or change accepted replay labels as part of the transport/UI continuation. Historical research below remains evidence, not current work authorization.
Historical receiver follow-up: TCCT plugin/library logs contain all three anomaly IDs at logged times 15:06:01 and 07:06:01.038; preserve both clocks until timezone alignment is verified. ProdMessage has no matching IDs. This proves receiver-boundary arrival only. Logs were preserved at **08:36:36 UTC** in `/home/user/Case_Event/grp6_receiver_evidence_20260919_pZGgeO.tgz` (final character uppercase O); hashes and limits are in the [transcribed observation](results/vm_production/receiver_observation_20260919.json).
Production JSONL SHA256: `dcd2fda6e8b09dd9fb51f9c713098c756abd1d24caece813a709af6bcd8fa4af`; engineering: `274c1892e177dc6834bfd6931b8924c560e53fb9a5f367766434591fbedb4da3`.
Recorded production model SHA256: `752e7c6c52b0c6e50a2c7ab37b7705ad21ec43ec18df968866832d07ba09afe9`. Preserve image/model/evidence; do not repeat production merely to reproduce existing proof.
Earlier production run 1 lost deployment during session recreation and recovered mid-lot; run 2 captured only 317 events after late collection. Both are partial. Run 3 used five-second EdgeLog snapshots; its startup-only pre-lot process remains in `grp6_core_prod3_capture_all.jsonl`, outside the audited process.

## Requirements and architecture

The [competition brief](Question_20260919.pdf) describes anomaly analysis/reporting and stage-timed temperature prediction; our scope covers both. Page 6 requires `ActionManager.set_message(tc.testerId, message)`. Website/LLM are extensions, not stated prerequisites.
Scoring: scenario fit 10%, Gemini operation 25%, correct timing 25%, analysis method 15%, report presentation 25%. Deadline/submission format, presentation duration, accuracy tolerance, physical units and effective TP timeout remain unconfirmed. No score is guaranteed.
Real-time path: `SmarTest/TP → Nexus → Edge ONEAPI → scoped features/models/detectors → ActionManager → tester`.
Active demo path: `Edge ONEAPI evidence → host EdgeLog polling → durable host SQLite queue → HTTPS ingest → D1 → snapshot/SSE → website`. Optional native Edge outbox remains disabled pending production storage/egress verification. Backend command polling/execution on Edge remains unverified and commands are disabled.
Edge inference/basic alerts must survive backend or LLM failure. Browsers never connect directly to Nexus or hold model API keys. LLM investigation runs in the background; synchronous predictions do not wait for external inference.

| Stage | Target / CSV pin | Latest eligible flow | Eligible features | Five-fold MAE (CSV units) |
| --- | --- | --- | ---: | ---: |
| 1 | `100_Main.sensor1_CP` / `sensor1#CP` | Suite1–14 + IDDQ_flow, before sensor1 | 25 | 0.003488 |
| 2 | `120_Main.sensor2_DS0` / `sensor2#DS0` | subflow1, before sensor2 | 525 | 0.026927 |
| 3 | `140_Main.sensor3_IO4` / `sensor3#IO4` | subflow2, before sensor3 | 1,025 | 0.020418 |
| 4 | `160_Main.sensor4_IO1` / `sensor4#IO1` | subflow3, before sensor4 | 1,525 | 0.024779 |
| 5 | `180_Main.sensor5_IO2` / `sensor5#IO2` | subflow4, before sensor5 | 2,025 | 0.063132 |
| 6 | `200_Main.sensor6_IO3` / `sensor6#IO3` | subflow5, before sensor6 | 2,525 | 0.027588 |

Order: request1 → sensor1 → subflow1 → request2 → … → request6 → sensor6 → subflow6. Use nested TP flows in source_review; test numbers/CSV order alone do not establish causality.
Current models exclude all sensor targets, final bins, pass/fail, total test time and wafer labels. Ridge selects ≤32 features; selection/imputation/scaling fit training folds only (wafer number modulo 5). Report held-out MAE/RMSE, baseline comparisons and wafer/site coverage; fitted replay error is not independent validation.
CSV row 0 begins PID/Lot/Wafer/Site/X/Y/PF/SBin/HBin/Test Time (3,046 columns); rows 1–4 are metadata, devices begin at row 5. Measurements use `<test number>_<suite>#<pin>`. SBin 1 is PASS; 2–32 FAIL. Preserve hex flags such as `0x0`.

| Evaluation label | First expected-category alert (completed devices) |
| --- | --- |
| W1 site imbalance | 32 |
| W3 / W9 low yield | 32 / 72 |
| W14 / W18 mean up / down | 32 / 32 |
| W23 / W25 spread up / down | 40 / missed |
| Remaining wafers labeled normal | W2 measures 53.75% yield: retain its valid low-yield alert despite label. |

Labels are evaluation metadata, never runtime rules or answers supplied to an online LLM. W6/12/15/21/24 had zero alerts but were inspected during tuning; they are not independent holdouts. Exact anomaly onset is unknown.
Detection combines normal baselines, per-test thresholds, related-suite agreement and persistence. Low yield uses completed-device SBin=1, minimum 32 devices, 80% threshold and a one-sided 95% Wilson upper bound before wafer end. Control multiple-testing false alarms; separate warming-up from normal and baselines from current-wafer windows.
W25 diagnostics found family gates below threshold and no qualifying family under normal-minimum ×0.7 spread screening. C evaluated a bounded spread-down candidate across all 25 wafers: W25 still missed; added alerts W1@56 and W23@80; scan cost increased. Reject promotion and retain production thresholds. These wafers are reused development data, not an independent holdout.
The later user-authorized sparse-burst source integration uses a separate normal-fit artifact `8e3c1f65432f78815f75eab566867f1ea1637b9eb2ee673262d946f250242872`; `runtime.json` remains `752e7c6c52b0c6e50a2c7ab37b7705ad21ec43ec18df968866832d07ba09afe9`. It counts devices with at least four tests above robust z=20 within a family, scans from device 72 every eight devices, and requires early bursts above the normal-fit maximum plus a one-sided Fisher p<=0.10. W25 has 6/36 early versus 1/36 recent bursts at 72, floor 5, p about 0.053. W15 exclusion depends on the scan start: it alerts at 56; leaving W07 out of calibration makes W15/W18 alert; fitting all 18 normal-labeled wafers delays W25 to 80. All 25 wafers were inspected during development. Local `RuntimeModels.detector()`/Monitor/rehearse now load this candidate automatically when the artifact is present; missing/invalid artifacts disable the supplement. This source behavior supersedes older local-runtime statements, while prior rejection records, the accepted replay and the deployed runtime remain unchanged. Synthetic specificity controls, dedicated unit tests and adversarial review are required before scientific/deployment acceptance.
R3 separately evaluated pooled within-site temporal variance with a family q95/two-scan gate. B independently reproduces six supplementary alerts, including W25@80, with no extra alerts on the 18 normal-labeled wafers and all original alerts retained. However, constant variance with a mean step produces false spread-down alerts in all 20 Gaussian controls; a single old impulse also survives overlapping scans. A reproduced both failures and rejects promotion. W25 disappears with q90/q97.5/q99 or three-scan persistence, so diagnostic 7/7 category matching is parameter-sensitive and is not accepted coverage. The final-device alert also lacks tester-delivery evidence. Preserve these unsuccessful results without changing the runtime artifact or accepted replay.

## Runtime invariants and machine protocol

1. Inspect `data.getType()` before legal getters; copy native values inside consumeData before return. Update current features before requests can overtake them. Preserve supplied SDK wrappers/native libraries.
2. Isolate run/tester/lot/wafer/device/site and verified head/attempt. Device identity is test UUID + site, separate from PartID; multi-head site collisions/mixed-scope touchdowns fail closed. Tests 20/21 decode lot ASCII chunks; test 25 decodes wafer even without WaferStart.
3. consumeTPRequest receives/returns strings. Java sends key=predict,data=N (1–6), AppLoc=edge, timeout=1 of unverified effective units. Reply `prediction N: (site,value) ...` through `set_wait(tc.testerId, 10, message)` then get; 10 is not an established deadline.
4. Feature wait releases the callback lock for ≤200 ms and rejects lifecycle changes. Never fabricate predictions for unknown stages/missing data or invent TP error shapes. Preserve per-site coverage, missing features, wait/lifecycle flags and latency; live timeout/failure acceptance is open.
5. Anomalies use set_message; key=prod_action retrieves get_prod separately from prediction get. Setter False is failure. Queued, returned-to-callback and tester-confirmed require different evidence; preserve final pending messages until retrieval.
   TCCT config runs msg at On_POSTBIN; receiver logs explicitly show a 2-second prod_action timeout. Prediction timeout units remain unverified. On_POSTBIN alone does not establish a poll after final-boundary alert emission.

Network/LLM/report generation stay outside callbacks; detector work and JSONL/stdout writes remain synchronous and require load measurement. Memory enqueue is not durable storage.
prod_action normalization preserves valid JSON byte-for-byte, escapes native string control characters without changing decoded text, and rejects other malformed responses with request_error/empty reply. The separate local TCCT compatibility step runs once afterward on acs_prod_var/msg/param=msg reasons: JSON-string-fragment encoding makes the observed raw FIFO interpolation valid while restoring original text at MessUI. Plain/unrelated responses remain unchanged; prediction/list paths are untouched. Adapted evidence retains raw_response, normalized_response, sent response, transport_adaptation and drained candidate IDs. returned_to_callback_unconfirmed never proves tester receipt. This workaround targets the observed writer and must be reconsidered if that writer gains correct serialization. Actual runtime/TCCT delivery is verified for three anomaly IDs in the latest complete host-relay run; final-boundary delivery remains unverified.
Raw logs include run/event/request/prediction/device IDs, process-local sequence, model SHA and scope. Only the first 12 mapped measurements are sampled: JSONL is not a complete measurement feed. Keep request_error/callback_error/action_error/report_error visible.
SDK getters supply HeadSite/site, test/suite/pin, raw value, unit/scaling, flags, limits, bins and coordinates. Unit/invalid-bit/retest semantics need live verification. Missing bin/setup.cfg is valid in the tested SDK; do not restore that installer prerequisite.

## Data formats and display semantics

| Boundary | Version | Time / discriminator / provenance | Source |
| --- | --- | --- | --- |
| Raw core JSONL | numeric `1` | ISO timestamp + Unix-seconds time; kind/tester/source_mode | grp6_app/monitor.py log() |
| Optional gzip exporter | string `1` | Unix-seconds timestamp; event_type/tester_id/mode | monitor.py event(), exporter.py |
| Normalized backend / adapter input | numeric `1` | timezone-qualified ISO timestamp; type/tester_id/source_mode | frontend/lib/rtdi/wire.ts, exporter-wire.ts |
| Internal UI view model | string `0.1-draft` | ISO sent_at and record times; records | frontend/lib/rtdi/contracts.ts, frontend/contracts/schemas.json |

Version 1 alone does not identify a format. Exporter-shaped batches accept numeric or string 1 through normalization; raw JSONL and retired nested DashboardEnvelope are not direct frontend batches.
Normalized envelope: `{schema_version:1,edge_id,batch_id,events}`. Each event requires event_id/type/run_id/tester_id/timestamp/source_mode. Types: measurement, prediction, prediction_actual, evidence, heartbeat, run_summary; source live/replay/simulation. Strict batches contain 1–100 events.
Use [wire validator](frontend/lib/rtdi/wire.ts), [wire schema](frontend/contracts/edge-v1.schema.json) and [formal fixture](frontend/contracts/examples/edge-v1-batch.json); the fixture is neither raw JSONL nor exporter format.
Prediction requires request_id/stage/device_id; actual requires request_id/actual. Join uniquely by run/tester plus applicable device/site; request_id alone cannot join across scopes. Raw requests have per-site prediction_ids/device_ids; preserve original request and per-site identity.
Legacy edge-adapter uses wire request_id as UI prediction_id: use per-site prediction identity there, retaining original request provenance without violating strict schemas. Same-ID/different-content is a conflict; UI conversion must avoid globally colliding IDs from scoped backend keys.
Alert payload: kind/message/test/site/completed_devices/observed/reference/score/series/site_series, baseline(mean/sd/thresholds) or null for yield, suggestion and optional family score/persistence. Score is not probability.
Replay summary carries mode/live_integration/wafers/expected_anomalies_detected/expected_anomaly_wafers/max_scan_ms/max_model_ms/prediction_in_sample_mae/validation.metrics/limitations. Wafer entries contain devices/yield/expected/expected_first_device/alerts; metrics contain n/mae/rmse/worst_error/baseline_mae.
Series indices are completed-device order within each site, not timestamps or spatial maps. Yield is 0–1, displayed as percent. Normalize raw string sites/number wafers at boundaries: formal site_id is numeric, lot/wafer IDs strings. Unknown scope remains unknown.
Coverage=1 means selected inputs present, not perfect accuracy. Empty predictions mean unavailable, never zero. Units stay CSV units/unverified; synthetic °C fixtures prove no machine units. Replay timestamps are ordering aids; saved live provenance does not imply a current stream.

## Implemented local API and UI

Routes below use `/api/v1`. The VPS demo is reachable through public HTTPS and its local nginx Docker container. Actual host-relay transport is verified; direct Edge-container export remains disabled/unverified. **No user authentication exists** on snapshot/SSE/incidents/chat/command creation; same-origin checks are not authentication. On September 19, 2026, user-supplied AI/ingest settings were installed in private VPS `/opt/grp6-preview/shared/.dev.vars` (root:grp6-preview, 0640), outside Git and persistent across releases. The deployed readable-UI release uses private model setting `gpt-5.6-sol` with medium reasoning in request construction; commands remain disabled. Public GET /api/config verifies the model and configured key/DB, without paid generation. After restart, localhost homepage/config returned 200 with AI, ingest and DB configured. Zero paid OpenAI calls were used; key validity/model access remain untested. The user requested sparse API usage: avoid automated paid smoke tests or repeated AI requests. An earlier HTTPS client check encountered a certificate error; September 19 public Chromium verification of the engineering release passed with normal TLS validation. Production requires user/team/run authorization and durable rate/cost controls.

| Method / route | Current contract |
| --- | --- |
| POST /events/batch | Bearer INGEST_TOKEN; durable D1 ingest, scoped event/batch idempotency, accepted/duplicates/rejected ID arrays; conflicts 409. Formal JSON ≤256 KiB; exporter ≤4 MiB compressed / 8 MiB decompressed, gzip supported. |
| GET /runs/{id} | Snapshot run/events/evidence/incidents/commands; optional tester_id; ambiguity 409, absent run 404. |
| GET /runs/{id}/events | Optional tester_id; Last-Event-ID or cursor query; ready/edge_event/heartbeat/stream_error; D1 row cursor, 25-second cycles, 10-second heartbeat. |
| GET /runs/{id}/measurements | Required tester_id/event_id; offset ≥0 (max 100,000), limit 1–100 (default 50). Returns metadata/measurements/total/offset/next_offset; 422 invalid query, 404 missing scoped bundle. Raw reconstruction ≤8 MiB/17 chunks; response <256 KiB. |
| GET /incidents/{id} | Optional run_id; **no tester_id selector**. Repository scopes incident/evidence; unresolved ambiguity 409. |
| POST /runs/{id}/chat | mode=openai/question, optional language=en or zh-TW (default en), tester_id/incident_id/history; persists answer/evidence IDs/tool trace. Same-origin guard permits absent Origin; no user identity. |
| POST /runs/{id}/commands | Same-origin Origin, user_confirmed:true, request_id/incident_id/kind=show_message/message ≤500 chars; optional tester_id query; live runs only, TTL 30–300 s (default 120), idempotent request ID. |
| GET /commands/pending | Separate bearer COMMAND_TOKEN; tester_id required, run_id optional; unexpired queued commands. |
| POST /commands/{id}/results | COMMAND_TOKEN; ack_id/run_id/tester_id/status/occurred_at, optional receipt/detail; idempotent ACK and forward-only transitions. |

States: queued → received → queued_to_tester → tester_confirmed, plus rejected/failed/expired. Transitions may skip intermediate stages; command-contract.ts is authoritative. tester_confirmed requires tester_receipt_id, with real correlated evidence needed for live acceptance.
R2 command creation resolves concurrent identical requests to one stored command without extending TTL; conflicting identity rejects. ACK insertion and status update recheck current scope/state and SQLite server time in one transaction. Backdated client occurred_at cannot revive an expired unreceived command; already-received commands retain late forward outcomes and exact ACK retries remain idempotent. Local SQLite interleavings/rollback pass; hosted D1 concurrency and real execution remain unverified.
Dashboard labels, UI contracts and validation use all seven backend states, including rejected. Snapshot commands include the receipt from the latest persisted ACK matching command/run/tester/current status, or null. Displayed confirmation is a backend report, not independent machine proof.
LLM tools are read-only get_run_summary/get_incident_evidence/compare_sites/get_prediction_records, bounded to six calls and a 20-second model/body/tool deadline. R3 pins resolved tester scope, rejects query/body mismatch and missing scoped incidents before model work, requires a successful tool before an answer, and persists success/failure evidence and traces. Storage operations outside the agent have no total request deadline; a database outage can prevent failure persistence. Cite only returned run evidence/event IDs and bundled KB reference IDs, keeping source types separate; distinguish facts/hypotheses/actions; treat source text as untrusted. No AI tool sends commands or silently falls back to fixed-rule answers. Chat accepts the request origin or exact server-configured APP_ORIGIN, never arbitrary forwarded headers; missing Origin remains permitted. This is not user authentication.
The local website assistant now provides Selected analysis and Semiconductor Q&A modes with a shared English/Traditional Chinese response selector, persisted in browser localStorage and applied on the next submission. Prompts are in `frontend/lib/rtdi/assistant.ts` and `agent.ts`; the seven locally authored concept/method notes are bundled in `frontend/lib/rtdi/local-knowledge.ts`, with provenance/maintenance in `frontend/knowledge/README.md`. Both modes receive the pack directly; no web search, remote document retrieval, embeddings, classification call or translation call is used. Generation still calls the configured model API. Knowledge notes are educational context, not independently reviewed specifications or current-run evidence, and omit wafer evaluation answer labels. Unknown references are rejected; cited notes are returned separately as knowledge_sources and expandable in the UI. Analysis retains mandatory verified tools, source/scope guards and persistence. Its prediction tool returns at most 100 stored prediction/actual records, optional stage/site filters, truncation status and event citations; it preserves repository-validated joins and does not establish whole-run accuracy from a subset.
General Q&A uses non-v1 `POST /api/assistant` with mode=openai/topic=knowledge/question, optional language=en|zh-TW/history, and no run/tester/incident/context. It requires no D1 connection and exposes no data tools; mixing run context with this mode is rejected. General chat history is separate, in page memory only, survives topic switches but not page reloads, and is not a persisted investigation. Analysis history retains local reference IDs alongside evidence citations. The small local pack is versioned with the deployed application; reference edits require a rebuild. Existing same-origin and process-local six-per-minute limits apply; public auth/durable quota gates remain open. Combined local verification after integration with the readable-UI release: 159 tests, TypeScript and production build passed. Browser checks confirmed the assistant on /workspace alongside the shared navigation and Temperature tab; prior local checks confirmed both modes, unscoped general-question drafting, retained drafts on mode switches and persisted response-language choice. Zero paid model requests; model answer quality/access remain unverified. Public deployment and UI/config checks are recorded in the current release verification.
Run workspace `/workspace` resolves the browser-tab source before mounting backend behavior. An imported replay summary opens an offline workspace with its filename, wafer/alert selection, supplied series, validation and source fields; it does not restore a previous backend scope or open SSE. The Semiconductor assistant remains available for general Q&A only; archive evidence is not sent to scoped analysis. Explicit general questions call the configured model without archive/run context. Individual temperature records, commands and receipts remain unavailable when absent from the summary. Explicit Load backend run switches to scoped snapshot/SSE, displays source/site evidence and persisted AI, refreshes commands on heartbeat, retains data while reconnecting and clears failed lookup scope. Prediction/command displays are read-only; no command-creation UI exists.
The local overview shows Incidents, Evidence records and Temperature predictions as three prominent numeric cards, with source-event and matched-actual totals subordinate. Loaded run/tester identity and the last source timestamp appear in a separate row; changing the picker does not change this row until explicit Load succeeds. Secondary selection metadata is inside keyboard-operable Selection details. Display timestamps include seconds and timezone, retain machine-readable ISO values and preserve replay import-order provenance. Mobile cards stack with right-aligned counts. This September 20 redesign passes final local validation; the VPS status row records its deployed revision.
R2 lifecycle generations reject late snapshot/error/AI responses even when transport ignores abort. Submitted scope changes clear old data/citations/commands; same resolved run/tester reconnect preserves saved data. Stream refreshes serialize/coalesce, successful snapshots clear stale errors, and evidence insertion does not silently change the selected incident. Helper tests exercise deferred transport; responsive/keyboard/browser interaction acceptance is separate.
The engineering workspace uses English or Taiwan Traditional Chinese operational labels, a compact run summary, readable evidence panels and light/dark/system themes. All three pages share one header in demo order: Wafer Analysis, Run workspace, Sandbox. The language selectors beside the theme control and inside both the guide welcome and active steps share one browser preference for interface labels, the guide and new assistant requests. A validated preference cookie initializes server-rendered labels for native page navigation and reload; legacy localStorage preferences reconcile before paint, temporarily hiding mismatched text until hydration, with a post-load fallback if initialization fails. Bundled translations make no API calls; source messages, raw records and historical answers remain verbatim. Sandbox rule-based narratives follow the same language locally. Prose/inputs use 16px and operational metadata/axes at least 14px; replay totals wrap without overlap. A large background 3D wafer with a flat orientation edge across its face and rim faces the mouse and rotates with scroll, with pause/top-view controls and reduced-motion/visibility safeguards. Its 400 illustrative dies reflect the selected wafer fail share rounded to 0.25 percentage points; positions are not measured defect coordinates. Sparse background dots span all three routes. The replay detail surface is 90% opaque; clicking the selected wafer toggles that panel without clearing selection, and selecting another wafer opens it. The Temperature tab emphasizes separate predicted/actual values and signed differences; repeated Unit unconfirmed labels beneath values are omitted, while supplied units and measurement/conflict status remain visible. Provenance/coverage/receipts remain expandable and the source-unit/tolerance note remains explicit. The healthy availability banner is removed while service errors remain visible. The assistant heading omits the Read only badge in both languages; its composer uses one outer keyboard-focus outline. Replay footer icons align with the center of their accompanying text, including wrapped lines. Native page anchors avoid the reproduced Vinext client-navigation failure. Question templates fill a draft; submitting is the explicit investigation request.
The source-aware guide opens automatically on a browser's first visit to any page, remembers dismissal/completion and reopens from Guide. It highlights each target's full visible bounds; alert selectors exclude asymmetric layout padding so the highlight centers on the controls, and large targets permit the prompt inside the highlight rather than shrinking the selection. Static examples are clearly synthetic and send no requests. Keyboard focus is contained, Escape closes, reduced motion is respected and cross-page progress uses session storage. Guide chapters follow Analysis → Workspace → Sandbox, use chapter-relative step counts, match current controls and adapt to backend, imported archive or no-data states. Navigation preserves the selected source and saved imports, never auto-loads data, and guards sandbox data and unfinished workspace drafts. Denied storage falls back to session or in-memory behavior; when both stores are unavailable, first-visit state cannot survive a reload.
Conversations and drafts are isolated by resolved run/tester/incident (event or run fallback). Tab changes, search, incident switching and same-tab route/reload navigation retain the appropriate log and citations. Restoration requires a fresh valid backend snapshot; canceled requests remain retryable drafts and late responses cannot cross contexts. A versioned sessionStorage cache is bounded to 2,000,000 serialized characters; denied/full storage falls back to memory. This is browser-tab persistence, not cross-device history. D1 investigations persist but server history listing is not exposed.
Visible Analysis navigation, titles and Guide chapters use Wafer Analysis (晶圓分析); the /replay route remains stable. Sandbox built-in messages, incident titles and units follow the interface language, including language changes; imported source text remains verbatim.
The homepage / opens Run workspace. The RTDI header logo opens /replay without the Test Analysis subtitle. /replay defaults on first visit to the bundled summary.json archive with 25 wafers, alert evidence, model validation and limitations. One picker has two groups: Replay contains summary.json, recorded Gemini captures and imported replay records; Live contains the two saved Gemini host-relay scopes. There is no separate Analysis source toggle. Selecting a backend scope opens its wafer overview with snapshot/SSE and original timestamps; selecting summary.json restores the bundled archive. Archive mode does not open SSE. The shared Workspace picker offers the same Replay/Live groups including summary.json. Explicit Load validates and saves the bundled summary, then opens the offline Workspace and disposes its previous backend stream; backend selection returns to scoped snapshot/SSE. Live classifies source provenance and does not assert that a completed run is still active. The 3D yield illustration, lot/wafer groups, selected-wafer yield/device/alert/prediction/actual counts and Yield & measurement coverage remain on Analysis. Individual incident evidence, site series and temperature tables belong in Workspace; the explicit link carries the loaded run/tester there. With records present, wafer groups use exact source identities, never a run-metadata-inferred lot/wafer pair; Defaults prefer identified wafers with supplied metrics over unknown or metadata-only groups; source updates can promote the automatic default while retaining explicit user selections. Empty snapshots may display a metadata-only placeholder without metrics or event time. Unknown IDs and missing yield remain unavailable. Supplied run-summary yield and completed-device counts are validated and displayed; absent yield remains unavailable. Existing stored projections were repaired explicitly with backfill-run-summaries.mjs after a SQLite-safe backup: 44 summaries scanned, 23 updated, and zero eligible on repeat. Raw hashes/scope were checked; original IDs, times and cursors were retained. The helper defaults to dry-run and resolves the current-release symlink correctly. Backend SSE connectivity is separate from machine freshness. The verified grp6-hc-relay source is labeled Gemini host log relay; it preserves original source times and does not independently poll tester health. Future source timestamps show freshness unverified (about 22 seconds of source/browser offset was observed). Recorded captures remain explicitly replay with original machine timestamps. Replay remembers its explicitly chosen bundled/imported/backend view across full-page navigation, independently of Workspace source mode. Choosing the bundled example retains saved imports; successful import/bundled loads synchronize the picker, and failed loads retain the prior view. Previously saved browser-tab imports remain available in Workspace; accepted replay artifacts are unchanged. `/sandbox` provides four bilingual synthetic practice scenarios: healthy, site imbalance, mean drift and missing/late predictions, with a late-actual action, draft questions, reset and rule-based explanations requiring no model call. Non-v1 /api/assistant and /api/config remain A-owned. Accepted replay still records W25 as missed; the separate source candidate is not promoted by a frontend release.
Run workspace reads normalized evidence directly; the adapter now retains 320-point series/site series, scalar thresholds, coverage, receipts and the original source batch. UI records and displays retain original_request_id/source_event_id; UI validation aliases the canonical wire validator. Snapshot prediction joins require compatible run/tester/source/request and supplied device/site/stage/lot/wafer/attempt/original-request scope, with one distinct matching actual event. Equal-valued distinct actuals remain ambiguous; exact event retries deduplicate. Scalar thresholds remain separately labeled until their scale is verified. The seeded workspace run now includes 24 predictions/actuals (six stages across four sites); the retained offline summary artifact is distinct from the persisted prediction seed.

## Exporter limits and live enablement

Unset GRP6_EXPORT_URL disables exporting. Otherwise callbacks enqueue copied events; a worker commits SQLite then sends gzip HTTPS with stable IDs. Plain HTTP is allowed only for localhost.
Config: GRP6_EXPORT_URL (actual HTTPS /api/v1/events/batch), GRP6_EXPORT_TOKEN (secret), GRP6_EXPORT_OUTBOX=/tmp/grp6_export.sqlite3, GRP6_EDGE_ID=grp6-edge, GRP6_EXPORT_QUEUE=32, GRP6_EXPORT_BATCH=1, GRP6_EXPORT_TIMEOUT=5. Verify persistent storage before relying on /tmp.
DeviceCompletedBundle includes full measurements, raw units/scaling/limits/flags, head/site/coordinates/bins, part/device and quality counts. attempt=null / attempt_status=unverified_sdk_field is intentional; live getter semantics/completeness remain unverified.
Lot-start detector_baseline_artifact includes runtime SHA, 3,035 baselines, test/family thresholds and baseline wafers. Legacy mean is nanmedian, labeled median_stored_in_legacy_mean_field; training counts/missing rates/build time/live units are unavailable.
Backend retains exporter payloads in raw_events/raw_event_chunks as base64 chunks ≤500,000 raw bytes. Alerts project to evidence/incidents; other source records remain run_summary. Prediction requests/actuals also project to scoped per-site records in the same transaction; ACK IDs remain original exporter IDs. request_id is per-site identity, original_request_id preserves the callback request, source_event_id preserves its source. Missing actuals remain raw only; unknown units remain null. Device bundles are available through the scoped measurement API. Existing normalized summaries are not automatically backfilled. The explicit validated run-summary repair adds missing count/yield fields without changing raw hashes or SSE cursors; duplicate event/batch retries do not perform that repair.
R2 exporter hardening deep-copies nested callback data into a bounded queue; successful submit is a non-durable enqueue, not delivery. The worker retains dequeued events until SQLite COMMIT succeeds. Only matching-batch, known, disjoint accepted/duplicate ACK IDs retire as delivered; rejected IDs move atomically to quarantine, and omitted IDs retry. Batch IDs are content-derived and stable for identical edge/content/order across retries and restarts; partial ACKs or splitting change batch membership/identity, while event IDs remain stable.

Invalid/nonfinite local or legacy persisted payloads, local identity conflicts, remote rejections and HTTP 400/422 are preserved in SQLite quarantine rather than silently normalized or discarded. HTTP 409 isolates named source IDs, or the full attempted batch when attribution is unknown; HTTP 413 splits batches and quarantines oversized singletons. HTTP 401/403 retain payloads with an auth-required reason and at least 30 seconds delay; 429/5xx/network failures retry with exponential jitter and bounded Retry-After. Redirects are refused. Logs contain reason codes, never response bodies, credentials or exception text.

Validation: `python -B -m unittest discover -s grp6_app/tests -v` passes 75 tests in the September 20 integration, including 22 exporter tests with real localhost lost-ACK/duplicate and outage/restart cases and four R3 prediction-seed tests. These are local tests, not actual-container transport acceptance. Queue capacity is bounded by event count, not bytes; deep-copy callback cost remains unmeasured on Edge. SQLite/quarantine disk use is unbounded and requires operational monitoring/manual review; no repair/requeue CLI is supplied. Shutdown attempts a queue flush with bounded caller wait; process loss before COMMIT or unavailable storage can still lose volatile submissions. Completeness/live SDK semantics and actual-container TLS remain open.

1. A/D establish permitted export/redaction, HTTPS deployment, D1 migration, token delivery/rotation and raw-retention/observability ownership; never commit credentials.
2. A deploys and verifies the locally tested ACK/recovery behavior above, with persistent outbox storage and quarantine review. Auth/identity/oversized-event problems require operator correction; local tests alone do not enable live exporting.
3. D/E verify raw-to-normalized-to-display scope, units, predictions/actuals and command semantics locally; A integrates runtime changes.
4. A tests actual-container DNS/TLS/gzip near-limit device delivery, restart/outbox recovery and local inference during outage. Development SSH alone proves no app-container egress; use an approved HC relay or honestly labeled manual export/replay if needed.

## Local runbook

The active host bridge is [deploy/host_relay.py](deploy/host_relay.py); [operational details](deploy/host-relay-README.md) define ACK/retry, conflicts, retention and private configuration. Uploaded ZIP `grp6_host_relay_20260920.zip`: 18,118 bytes, SHA256 `6738ac241a6cbc450e5bc1800a69fa67ec2171a49ef8f69c6c7123c80f102c1a`. Actual group-6 Python 3.6.8 passed 27 tests with one parser-feature skip. Release: `/home/user/Case_Event/grp6_host_relay_release_20260920_GlNvAM`. Private config: `/home/user/.config/grp6-relay/config.json` (0600); state: `/home/user/grp6-host-relay-state` (0700). Never print/copy the token into docs or Git.

Host relay runs under nohup, not as a boot service. Verify relay.pid against `/proc/<pid>/cmdline` before stopping it. With umask 077, restart with the same state/config: `nohup python3 -u -B <release>/deploy/host_relay.py --state-dir /home/user/grp6-host-relay-state --send --config /home/user/.config/grp6-relay/config.json >> /home/user/grp6-host-relay-state/relay-live.log 2>&1 < /dev/null &`; record `$!` in relay.pid. The state lock rejects a second process. Preserve the stopped SQLite database and sidecars before maintenance; host Python 3.6 lacks Connection.backup. Never delete the ledger to reset counters. Polling can miss records rotated before collection. A host reboot requires manual restart. No Nexus restart is needed.

For the demo, confirm group-6 is bound/idle and the relay is running. Select its run in Workspace and watch original source timestamps and prediction/incident counters. Start the already-bound supplied recipe only when needed: `DISPLAY=:1 XAUTHORITY=/home/user/.Xauthority /opt/hp93000/testcell/bin/tcct -m -r /home/user/Case_Event/SmarTest/recipe/acs_tcct_4site_ft.xml`. Refresh discovery and explicitly load the newly created run; the selected source never changes silently. Analysis shows the same backend run and SSE feed as a wafer overview; choose recorded replay or summary.json from the stored-run picker. Sandbox is synthetic practice with no tester/network/model side effects. Supplied-program repetitions remain ORE input, not independent physical measurements.

Node ≥22.18; lockfile governs dependencies. Python runtime/replay use stdlib; training needs NumPy; native ONEAPI belongs in supplied Python 3.10.
Keep OPENAI_API_KEY/OPENAI_MODEL/random INGEST_TOKEN in ignored frontend/.dev.vars; COMMAND_TOKEN is separate. For a TLS proxy set APP_ORIGIN to its exact public origin without a trailing slash. Restart after configuration. No secrets in examples, browser bundles, NEXT_PUBLIC_/VITE_ variables, docs or logs.

1. In frontend/, use `npm ci` if dependencies are absent, then `node scripts/local-backend.mjs migrate` and `npm run dev -- --host 127.0.0.1`.
2. In a second frontend terminal, run `node scripts/local-backend.mjs seed`; localhost-only, retry-safe, no AI call. Load `http://localhost:5173/workspace`, run grp6-replay-demo / tester grp6-replay.
3. Verify frontend with `npm test`, `npx tsc --noEmit`, `npm run build`. A verifies core with `python -m unittest discover -s grp6_app/tests -v` from root. Record actual revision/results, not inherited counts.
4. A can regenerate replay with `python -m grp6_app.rehearse source_review/training/Data --output results/replay`; frontend `node scripts/sync-replay.mjs` validates/copies public data with source SHA. These overwrite A-owned outputs; B/C output only inside their workstreams.

`npm run replay:edge -- --run-id <id> --started-at <ISO>` converts/validates deterministic evidence batches; no network without --endpoint. Replay times are synthetic ordering. build_models/calibrate overwrite default artifacts: only A promotes reviewed candidates.
After integration, C's evidence/ownership check is `python -B -m workstreams.detection.verify --contribution-ref 5f403bd`; this scopes ownership to C's delivered commit while checking current evidence. B/C auditors accept only LF/CRLF equivalents for source files when a raw hash differs; data/model/output hashes remain exact and recorded evidence is unchanged.

## VPS preview runbook

The user separately authorized SSH/uploads to `root@103.27.132.88`; competition-machine transfer restrictions below still apply to grp6. See [deployment procedure](deploy/vps-preview/README.md) and [verified results](deploy/vps-preview/verification.json). From this checkout, `./deploy/vps-preview/publish.ps1` fetches published main, archives committed frontend/replay inputs, uploads with the existing SSH key and invokes the verified updater. No repository credentials are installed on the VPS.

`grp6-preview.service` runs as nonroot `grp6-preview`, starts at boot and binds `127.0.0.1:5173`. The existing nginx container reaches its private socket proxy at `172.23.0.1:5173`; no public-interface listener or nginx configuration was added. Disable nginx response buffering for SSE. Releases live under `/opt/grp6-preview/releases`; shared D1 state and ignored secrets survive updates. Builds precede a brief stop/backup/migrate/activate interval; backup and activation failures were both verified to recover the preceding service/state. Retained backups/releases require eventual disk housekeeping.

Isolated Node 22.18 leaves global Node unchanged. The VPS CPU requires workerd-only `qemu-x86_64 -cpu max` emulation, so startup/requests are slower. Fourteen labeled replay alerts plus 52 content-identified source seed events are ingested idempotently, yielding 114 persisted source/projection events with 24 predictions joined to actuals and four measurement bundles in the training scope. The catalog also contains two authentic recorded captures and two Gemini host-relay scopes; current acceptance is recorded in [public release verification](results/continuation_20260920/demo_release_verification.json). AI/ingest settings are private and persistent; command token is absent. Source `63d5d1d61f926563953e0dabbe5423dda8c6265f` and its [continuation release evidence](results/continuation_20260920/vps_deployment.json) are historical. The earlier verified application implementation is `de8ff5c776d49fd032e4398d1c23353fef73c451`; VPS current/REVISION identifies the activated publication including later helper/docs/test updates. The updated helper stops the socket proxy during backup/migration/activation, restarts the app and verifies its process directory matches the new release before resuming the proxy. This fixes traffic restarting the old process during an update. Installed helper changes are separate from publish.ps1; preserve LF and run bash -n before installation. Current HTTP/invalid-chat checks used zero paid model calls. Host-relay acceptance includes the fresh Gemini run and correlated tester receipts; direct-container export remains disabled.

## Remote runbook — A only

Only grp6: verify group-6 and dashboard rows grp6_acs_host_controller/grp6_acs_edge_server. Never use grp1 results as readiness evidence. User handles all local/remote uploads/downloads; A prepares/verifies exact artifacts. Preserve originals and coordinate sessions; **never restart Nexus during SmarTest**.
Latest uploaded immutable release: `grp6_export_transport_20260920.zip`, **368,583 bytes**, SHA256 `6c30d89bcddf568cc47a7d0c6adbe2572e65723f367b82c8cd6f33c48a8d2b8b`. Verified on group-6 and extracted to `/home/user/Case_Event/grp6_transport_release_20260920_UlZCXF`; 56 packaged tests and real SDK Monitor construction passed before the versioned image push and AppDeployer stop/start. See [deployment observation](results/continuation_20260920/transport_deployment_observation.json). The preceding 337,747-byte `grp6_core_alert_json_fix_20260919.zip` and its logs remain historical evidence; immutable archives are never replaced under the same name.

| Resource | Recorded location |
| --- | --- |
| Host / Edge | 180.3.13.36 / 180.3.13.209; recheck named rows on reconnect. |
| Host package / SDK | /home/user/Case_Event; Edge/oneAPI_py3.10/bin; training/Data. Host Python 3.6.8. |
| Edge development | debugger@advantestcell.local:29022; code-server http://advantestcell.local:29080/?folder=/home/debugger/project; project → /data/project, SDK oneapi_sdk. |
| Container | Python 3.10.12, unifiedserver.local/all/template-data-app:v22.04; SDK/app /opt/nexus/OneAPI/bin, starts python3 -u main.py. |
| Descriptor | /home/user/Case_Event/SmarTest/app_descriptor.json and /opt/acs/nexus/conf/app_descriptor.json; py-app pinned to grp6/py-app:20260919T175744779996Z under unifiedserver.local. |

Dashboard: `https://sandbox.gemini.te-cloud.advantest.com/dashboard/virtual-machine`; recorded grp6 gateway tevmip-180-3-13-67-endtevmip. Rediscover tabs and use fresh VNC screenshots; typeText previously lost newlines, so verify entry. Host lacks rg; use grep/find/sed.

1. A packages with `python -B deploy/package_grp6.py --output <new-archive-path>`; exclusive archive creation and CRC/per-file SHA checks include SYSTEM.md. The release allows only runtime.json, manifest.json and validation.json artifacts, pins runtime SHA 752e7c6c52b0c6e50a2c7ab37b7705ad21ec43ec18df968866832d07ba09afe9, and omits sparse_burst.json. The imported sparse_burst.py dependency is included but its supplement stays disabled without calibration. Existing ZIPs are unchanged historical evidence.
2. After user upload to /home/user/Case_Event, verify hostname, sha256sum, stat -c %s and unzip -tq; stop on mismatch. Extract to a new unused directory; run `python3 <release>/install_grp6.py --build --push > <new-log> 2>&1`; retain grp6_deployment_latest.json/digest.
3. Installer checks group-6/hashes/exact runtime manifest, copies SDK without stale grp6_app to unique staging, edits staging main.py, builds a versioned image, runs packaged tests and constructs real Monitor before push. Only the versioned tag is pushed; latest is untouched. Smoke checks verify the accepted model and absent candidate calibration. Optional private export-config/descriptor preparation stays outside bundle/SDK/build inputs, preserves supplied storage fields, and never applies the descriptor. See [private configuration procedure](results/continuation_20260920/exporter_preparation.md). Records distinguish built, pushed and descriptor_prepared from deployed=false. Avoid destructive supplied tag.sh and stale grp6_app_upload*.zip scaffolds.
4. Preserve a healthy bound, idle session. Installed `/opt/acs/nexus/bin/AppDeployer --help` verifies `stop` purges and `start` deploys; 0 means success. Preserve Edge logs first, stop, verify removal, start, then check fresh Monitor and ACS Nexus GUI Information → Container Status (hover py-app for image:tag). Host Docker/registry digest alone is not actual Edge digest proof. Never clear internal state or restart Nexus to guess at recovery.
5. Only if session reconstruction is necessary, in /home/user/Case_Event/SmarTest run `DISPLAY=:1 XAUTHORITY=/home/user/.Xauthority bash runTp.sh load`; it recreates the workspace, kills SmarTest/TCCT and attempts AppDeployer start. Wait for SMT8 Ready and complete final image activation afterward. `bash runTp.sh eng_run 1` is engineering; `bash runTp.sh prod_run` also rebuilds the session and ignores its second argument. Prefer the already-bound production recipe after verifying readiness; recorded run 3 avoided launcher teardown this way.

runTp.sh copies descriptor each invocation. Save workspace/descriptor/deployment first; recorded backups: grp6_before_prod_workspace.tgz, grp6_deployment_before_core.json, grp6_descriptor_debug6.json. Collection used ONEAPI_DEBUG=1; supplied default 6 obscured early evidence.
Register Monitor before connect; zero connect return proves initiation only. Evidence defaults /tmp/grp6_evidence.jsonl and .html; GRP6_EVIDENCE may target verified persistent storage. Preserve before removal; reports refresh asynchronously every 32 devices/boundaries and drain at shutdown.
The September 20 current descriptor has only edge.containers; py-app fields are environment/image/metrics/name/requirements, with no observed mount configuration or export URL/token. Workshop developer `/home/debugger/project` persistence does not establish production-container durability. Before enabling export, verify the supported production storage mechanism, writable directory including SQLite sidecars, and survival across AppDeployer purge/replacement. An absolute outbox path alone is not durability evidence. Actual-container HTTPS/auth/ACK and original-ID DB/snapshot/SSE correlation remain required; local retry tests do not close these gates.
Collect `Edge/EdgeLog/EdgeLog log` periodically (five seconds captured run 3); end-only retrieval truncates, and --help also collects. Strip GRP6_EVIDENCE prefixes before JSONL auditing.
Report: `python3 -m grp6_app.report <jsonl> --output <report.html>`. Audit: `python deploy/audit_evidence.py <jsonl> --output <audit.json>` (bundle: audit_evidence.py at root). Require six stages/all sites/full coverage/no errors and separate tester EDL/UI receipts; callback audit alone is insufficient.
VM replay mounts host training/Data read-only and a dedicated output directory in the versioned image, overrides entrypoint to python3, runs `-m grp6_app.rehearse /data --output /out`. It needs no native connection/NumPy and must stay labeled replay.

## Provenance and retained references

| Historical artifact | Recorded identity |
| --- | --- |
| Core upload | 331,948 bytes; SHA256 c49930d9a7ecb55ad4a77a38a705c58095406be234572c21558bc4e8d07d762c. |
| Channel fix | grp6_channel_fix.zip, 311,583 bytes; SHA256 21c52ec6bd795414b61da86c8c49ba78f1e0cbab8a7466100bb7c69c13ffb270; tag 20260919T052422Z. |
| Original bundle/image | 310,207 bytes; ZIP SHA256 710f533122c86dfb43c847196e906de5b3d0b2f7242db21b2be604445eb22243; tag 20260919T044025Z, registry digest sha256:f7c19251a2c7eb7c106c7d762e38ba03d590f3e06f722a0c419fc543d1003efd. |
| Hex-flag image | livefix-20260919; digest sha256:81cd96f70948955b3010ba454e06ea43db68fc816278368607b5e77f787c4057. |

Keep [Question PDF](Question_20260919.pdf) (p3 labels/targets, p4 timing, p6 messaging), [Workshop PDF](WorkShop_Material.pdf) (p11 transfer, pp19–26 deployment, pp28–29 data), [ONEAPI Manual](ONEAPI_Manual.pdf) (Monitor/NexusData/ActionManager/lifecycle), tmp/pdfs extracted text and SDK/TP sources. Diagrams require original PDFs; supplied py-app.log is reference output only.
Keep data/models/packages/results/diagnostics, [frontend license](frontend/vendor/shadcn-tailwind-4.13.0.LICENSE.md), all skill files/licenses and [provenance](.agents/skills/i-have-adhd/UPSTREAM.json). Skill upstream b15d0be58f55b33972ba3e39709e0e5208ef30cb; AGENTS explicitly invokes it without modifying opt-in metadata.
Verified grp6_sources_data.zip: 12,571,563 bytes / 66 files in source_review. Earlier ~525 MB full archive's first local copy was truncated; do not use that partial extraction. Root/SDK requirements duplicates are intentional.
Superseded docs are backed up at C:/Users/USER/AppData/Local/Temp/grp6-docs-before-system-20260919; they are historical context, not current authority. Existing packaged docs remain revision-specific.

## Current orchestration round

Round **R3-20260919** is prepared in all four team briefs for user dispatch. R2 is accepted below; current R2 COMPLETE notes remain until each worker starts R3 and do not count as R3 delivery. Its exact base is the published handoff commit supplied at dispatch (the commit containing those briefs); each worker records it in notes. After consolidation A uses C:/Users/USER/Documents/hackathon2026 on main. Only remote main is published.

The legacy first round is accepted at ec1bafc, with original D history and final E notes preserved through fd7fe29. Its explicit A dispositions and results/integration_20260919.json are the accepted migration exception to the newly introduced matching-round/COMPLETE markers; old assignments are not silently rerun. B 86445d7, C 5f403bd, D 3e5b829 and E final notes 06887ad are reachable from main. The recorded 128 tests, TypeScript/build and evidence audits apply to that accepted code; orchestration-only edits do not imply fresh machine checks. Prior notes remain at fd7fe29:workstreams/<role>/NOTES.md.

R2 ownership: B audits readiness/timing evidence and missing machine cases; C profiles detector cost and evaluates at most one output-equivalent optimization; D hardens atomic command lifecycle/expiry guards; E tests and fixes asynchronous dashboard scope/reconnect behavior. A handles exporter ACK/recovery, independent integration review and grp6 image/receipt verification. Workstream code, metrics, logs and proposals remain needed for reproducibility; no cleanup deletion or model/detector promotion is authorized by these assignments.

R2 is accepted at integrated code revision `ba89a991b6ca612917ef87ba0f54c5c3723d7867`: B `67d5462`, C final handoff `0052c52`, D `25d013b`, E `aeb4209`, and A exporter `4f54907` are included. All four matching COMPLETE notes and authored allowlists were reviewed. Combined checks pass 203 tests (66 core + 36 B + 20 C + 81 frontend), TypeScript and build; B readiness/prior evidence and C historical/profile aggregates/hashes were independently audited. Independent static D/E review found no actionable defects. [Integration record](results/integration_r2_20260919.json) preserves exact revisions and limits. Accept B's audit and C's deferred result; retain the current model/detector. This is source integration acceptance, not new Edge deployment or live transport/receipt acceptance.

R3 follows the user request for W25 detection and complete polished frontend features. B independently audits spread signals and false alerts; C implements/evaluates a general spread-decrease candidate; D completes scoped backend flows; E polishes and verifies UI flows. A runs orchestration/core integration and deployment. B/C are local subagents with disjoint file scopes; the user dispatches D/E. After preserving all accepted work, A consolidates this machine to the primary main checkout and removes merged auxiliary branches/worktrees. For this round A alone commits/publishes local A/B/C changes; B/C do not create worktrees, branches or run mutating Git. D/E retain their separate external checkout workflow. This explicit exception overrides generic role-branch rules below. No candidate promotion without reviewed all-wafer/control evidence; no live machine claim from replay. Accepted R2 notes remain at ba89a99; workers replace their own notes on R3 startup. Workstream inventories support reproducibility, so no evidence cleanup deletion is needed.

R3 dispatch bases were B/C `810b4ac9549a619ab09e9145f656c363c6d75035` and user-dispatched D/E `7f99026b23a33a356c2470caa184eef9176f3034`. R3 source integration is accepted at `ad4a54aba21e49001f1a10c95a0636179b6cc116`, including D COMPLETE `972c349`, E COMPLETE `459992e`, A fixes, and both reviewed negative B/C research deliveries. B/C follow-up was published by A under the shared-main exception; recorded manifests and 44 indexed evidence files retain exact bytes. D/E are implemented and checked locally/VPS; this acceptance does not close the remaining machine/authentication/model-access gates. B/C are quiescent. Prior local cleanup retained only main; during final integration another worker created the locked `.claude/worktrees/w25-sparse-burst` checkout/branch. Preserve this potentially active work and the new untracked `prompts/message.txt`; they are outside this accepted release and must not be deleted as obsolete.

## Ownership and integration

A is core owner/integrator and owns everything outside explicit B/C/D/E allowlists: all core grp6_app, artifacts, deploy/VM, results/source data, root docs/files, team briefs, manifests/lockfiles/scripts/shared configs/examples and non-v1 API routes. D's db/drizzle migration files are the explicit schema exception; deployment configs remain A-owned.
B owns files under workstreams/prediction/; C owns files under workstreams/detection/, including their NOTES.md. Both deliver offline proposals; A reviews/promotes accepted runtime/artifact changes.
D owns frontend/app/api/v1, db, drizzle, contracts/edge-v1.schema.json, enumerated backend lib/rtdi files and backend tests in [D assignment](team/D_BACKEND.md).
E owns enumerated UI pages/components/styles/hooks/libs, UI schema and dashboard/contracts/replay/ui-* tests in [E assignment](team/E_FRONTEND.md). No blanket app/ or lib/rtdi ownership; importing a file grants no write permission.
Freeze assistant.ts exports instructions/demoAnswer/ChatMessage and D-consumed behavior/signatures; agent imports instructions, chat-handler imports demoAnswer. Preserve compatible EventView/validatedView contracts. A coordinates breaking interface changes.
Each teammate maintains only their own notes: B workstreams/prediction/NOTES.md, C workstreams/detection/NOTES.md, D workstreams/backend/NOTES.md, E workstreams/frontend/NOTES.md. D/E own these exact notes files in addition to their frontend allowlists. Update Progress, Decisions, Blockers and Handoff after meaningful work and before stopping; include commands/results, evidence paths, base/branch SHA and next action. Replace stale status rather than appending diary entries. No credentials, copied canonical contracts or unverified success claims. Record required outside-scope edits with path/reason/requested owner, continue independent work, and leave those files untouched. A promotes verified decisions into SYSTEM.md; teammates do not edit SYSTEM, AGENTS or team briefs.

1. Remote `main` is the sole publication branch. Each role works in its own checkout/local branch: `teammate-a-core`, `teammate-b-prediction`, `teammate-c-detection`, `teammate-d-backend`, `teammate-e-frontend`. A publishes a numbered round and exact handoff SHA; new work starts there, never at a permanently hardcoded old base. Preserve unfinished work and never switch another agent's checkout. Current assignments identify the active round; start only from A's matching published handoff SHA.
2. Author only allowlisted files. Audit your individual commits, staged edits and untracked paths; after synchronization a base-to-HEAD diff also includes others' contributions. Notes include round ID, status (IN_PROGRESS/BLOCKED/COMPLETE), base SHA, contribution SHAs, commands/results, evidence and owner requests. The final delivery SHA goes in the response, not its own commit.
3. Keep synchronization minimal: fetch once at task start and once immediately before publication (reuse a fetch just completed for that purpose). Merge `origin/main` into your local branch only when it is not already an ancestor of HEAD. Run assignment checks once on the final implementation; after incoming changes rerun only affected checks, preserving recorded results for unchanged code. Push explicitly with `git push origin HEAD:refs/heads/main`. Never publish local role branches or force-push. A successful push confirms publication; fetch again only for rejection, uncertain outcome or a new review boundary. On a race, fetch/merge/recheck only the new delta and retry; on a permissions/network failure diagnose instead of looping. Abort conflicts outside your allowlist and coordinate with A; never discard another owner's changes.
4. Teammates report COMPLETE only after checks and confirmed publication, then stop editing until A reviews or reassigns. A batches review after all four same-round handoffs arrive, verifies delivery commits are reachable from main, reads notes/outputs and runs combined acceptance once per round; repeat only for relevant fixes/new code. Main may contain work awaiting review: publication is not acceptance or deployment. A records the reviewed revision and open gates in SYSTEM, cleans only verified obsolete files while preserving reproducibility/rollback evidence, and rewrites all four team/ assignments for the next round.
5. Reusable instructions: [orchestrator prompt](prompts/ORCHESTRATOR.md) and [teammate prompt](prompts/TEAMMATE.md). Only A uses grp6 VNC/SSH and promotes runtime changes; the user handles local/remote transfers. Prefer completion notifications to repeated fetches. Without notifications, inspect for delivery no more than once every five minutes while doing independent work; an unchanged head requires no merge/recheck. Do not claim unattended continuation without an authorized scheduler.

Following allowlists prevents overlapping file edits. It cannot guarantee 100% semantic compatibility; shared interfaces still need integration checks. D/E verify shared interfaces; A integrates D/E with scoped snapshot/SSE/prediction/receipt checks and evaluates B/C proposals before core promotion. Assignments: [B](team/B_PREDICTION.md), [C](team/C_DETECTION.md), [D](team/D_BACKEND.md), [E](team/E_FRONTEND.md).

## Remaining acceptance gates

| Gate / owner | Evidence needed |
| --- | --- |
| G1–G2 / A, B proposals | Fresh host-relay run confirms 120/120 requests, 480 actual joins and 120 successful tester prediction executions. Prior 119/120 gap cause remains open. Units/scaling/flags/deadline and failure/retest/head/reconnect machine cases remain unverified. |
| G3 / C → A | W2/W25 investigation deferred by the user September 20. Source candidate remains unpromoted; historical specificity/independent-validation limitations remain. Accepted replay/deployment remain 6/7. |
| G4 / A | Scoped workaround deployed; fresh actual runtime/TCCT receiver Insert text confirms all three original anomaly IDs. Actual Edge digest and final-boundary message generation/later poll/receipt remain open; visible Message Center anomaly scrollback was not separately located. |
| G5–G6 / D/E → A | Public host-relay snapshot/SSE, evidence charts, predictions/actuals, repaired supplied yield and scope-preserving navigation pass. Five stored scopes and four Sandbox examples available. Full native measurement reports, public user authentication and durable quota acceptance remain open; direct-container transport is separate from the verified host bridge. |
| G7–G8 / A/D/E | Host relay → HTTPS ACK → persisted snapshot/SSE → public UI demonstrated during a complete fresh Gemini run: 1,723 events, 480 predictions/actuals, three incidents/evidence records. Native Edge exporter remains disabled; direct-container storage/egress and command execution are unverified. Host recovery and release rehearsal are recorded separately. |

Demo: load group-6 / 04dcb07358ee4f3da41e5cbc12cb9850 in Analysis → source/freshness and 92.50% supplied final wafer yield → Open this run in workspace → 480 predictions/actuals and three anomaly/site evidence records with truthful receipt state → Sandbox practice. The picker also offers recorded Engineering check (24 joins) and Production run 3 (480 joins); these preserve historical timestamps. The current run is complete/idle: new counters require another tester run; a connected backend SSE alone is not machine activity. AI is optional; no paid model rehearsal is authorized. The workshop_core_audit.md scoring review maps the brief to evidence and remaining gaps; full points are not guaranteed. W1/W3/W14/W23/W25 cases include explicit W25 no-alert state. Keep focus indicators, reduced motion and responsive evidence views; missing records never imply normal.
