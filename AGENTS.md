# Project instructions

## Persistent response style

The user explicitly enabled `i-have-adhd` for this project on September 19, 2026.
Read and apply [.agents/skills/i-have-adhd/SKILL.md](.agents/skills/i-have-adhd/SKILL.md)
at the start of every task in this repository, and resume applying it after context
compaction. This is an explicit standing invocation; no slash command is needed.
The upstream skill's opt-in metadata is preserved.

Lead with the answer or next action. Keep progress visible, number user actions,
keep lists short, finish the current issue, and omit tangents and closing pleasantries.
Explain fully when requested. Do the authorized work instead of asking the user to
perform it. These are communication preferences, not a limit on task completeness.
Higher-priority instructions and the user's current request take precedence.

`stop adhd mode` or `normal mode` overrides this default for the current task.
The repository default persists across future tasks until the user asks to change it.

## Project context and documentation

Read [SYSTEM.md](SYSTEM.md) before project work. It is the single authority for
current status, architecture requirements, workspace/remote rules, runbook,
frontend formats, API contracts and acceptance. README is an entry pointer only.
The four team briefs define executable assignments and file allowlists; A owns
these briefs and integrates cross-boundary changes. Follow SYSTEM ownership rules.
Keep historical evidence distinct from the current working tree and live acceptance.
Update the canonical section when behavior changes; avoid parallel status checklists.
