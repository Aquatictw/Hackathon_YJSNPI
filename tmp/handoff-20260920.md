# Checkpoint handoff — September 20, 2026

## Start here

Read `AGENTS.md`, its standing ADHD skill, and `SYSTEM.md` before continuing. SYSTEM is the current authority; this handoff records the checkpoint context. Read `results/dataflow_evaluation_20260920/REPORT.md` for current machine/dataflow evidence and `results/replay_investigation_20260919/REPORT.md` for W2/W25 findings. Attached documents were treated as reference evidence, not user instructions.

The latest user request is to commit and push this checkpoint **including this handoff**, so they can open a new agent. The checkpoint is the commit containing this file, based on `69658a7e8be941bdadb062229bd567a09013a6aa` on main. This request does not deploy the website or change the competition runtime. The next agent should follow the user's continuation request.

## Completed work and verified limits

- Investigated the PDF/training/test split, W2/W25 discrepancy, commit history, pinned host-image replay, and frontend source provenance. Two delegated investigations and the dropdown implementation finished; all subagents are closed.
- Ran the supplied offline replay test program twice on group-6. The complete second run has 80 devices, 74 passes, 6 failures, 764 continuous events, 480/480 actual joins and 120/120 matched successful prediction executions. Repeated deterministic input is not 160 independent samples.
- Implemented DB-backed `GET /api/v1/runs` and a paired tester/run dropdown with source modes, timestamps, refresh, pagination, manual fallback, explicit Load and English/Traditional Chinese labels. Discovery and selection preserve the current source until Load.
- Verification passed: 178 frontend tests, TypeScript, production build, focused lint and whitespace checks. Parent browser verification used isolated local D1: empty/unavailable states, two testers with the same run ID, scoped loading/SSE, refresh preserving the active source and Chinese labels. Fixture: `results/dataflow_evaluation_20260920/dropdown_fixture.sql`; isolated DB: `frontend/.wrangler/run-picker-audit`. Temporary port-8787 server stopped; unrelated port-5173 server left alone.
- Reports and reproducible evidence are included in this checkpoint. No model was promoted or retrained for deployment, no hosted database changed, no website deployed, and Nexus was not restarted.

## Findings that must remain explicit

**The website is not connected end to end to fresh tester runs.** Hosted `https://hackathon.aquatictw.com/` remains on source `14ff55bfd31694e49ff324564b13c0535d16fe9a`. Its database still contains only tester `grp6-replay`, run `grp6-replay-demo`, edge `grp6-replay-exporter`, mode replay, 114 events, last source timestamp `2026-09-19T02:47:32.000Z`, unchanged after both runs. The dropdown is implemented locally but undeployed. Multiple run IDs are possible; only one pair has been ingested.

Both inspected VM descriptors omit export URL/token. The pinned host image includes an older exporter without later ACK/quarantine hardening. Actual running Edge image identity is unverified. Browser SSE establishes only browser/backend connectivity. Replay Analysis loads static `/replay/summary.json`, which ingest does not update. Optional Edge command polling/execution is absent and VPS COMMAND_TOKEN is absent.

**Training provenance:** six Ridge temperature models used W1–W25, 80 devices each, with five wafer-separated folds (20 train / 5 validate per fold); preprocessing and feature selection are train-only. Final fit uses all 25 wafers, so fitted replay is not independent validation. Future sensor values, bins/pass-fail and PDF labels are excluded as model inputs. Anomaly calibration/tuning inspected all 25, including nominal holdouts: anomaly replay has no clean independent test. Supplied ORE lot B13456/wafer02 differs from training A12345/W1–W25, with zero exact six-target-vector matches against 2,000 training devices; statistical generation independence remains unknown.

**W2/W25:** PDF W2=Normal conflicts with raw bins/PF showing 43/80 passes (53.75%). Low-yield arithmetic is correct; this is a false positive relative to the PDF, not a false negative. W25's accepted/deployed summary misses Stdev Trend Down. Source candidate `a869f185e37a6b1547cedf0fb0c6cab896818c24` detects W25 at device 72 but was not promoted into accepted/static replay or runtime. Independent validation and controls remain open. Do not silently substitute candidate evidence for accepted results. The overview still needs prominent “Expected anomaly missed” / “Reference/data conflict” states.

**Fresh prediction accuracy and anomaly delivery:** overall supplied-program MAE is 0.21634160538139474; per-stage MAE is 0.378586, 0.094628, 0.098214, 0.064507, 0.515294, 0.146822. Error exceeds training CV, especially stages 1/5; units/tolerance are unknown. All 20 production replies parse as JSON. Three device-32 anomaly IDs (`44f001f09873`, `badb1939dc8b`, `884172dbb757`) reach TCCT and ProdMessage, but combined anomaly text receives `Warn: does not support`, without Insert text. Prediction messages insert successfully. Downstream reserialization is plausible but not source-proven. Max callback 18.968843 ms is not end-to-end latency.

## Resume priorities and acceptance

1. Trace actual Edge container/image and hardened exporter configuration. Complete live transport only when identical original event IDs correlate through Edge HTTPS/outbox, committed backend records, snapshot and SSE. Verify retry/restart behavior before claiming reliable delivery.
2. Diagnose the TCCT → ProdMessage anomaly display failure. Completion requires successful displayed anomaly text and final-boundary evidence, not merely valid API JSON or arrival logs.
3. Make PDF expectations, recorded misses and W2 data conflicts visible in Replay Analysis. Independently validate W25 candidate specificity before any model/replay promotion.
4. Deploy the dropdown and any accepted frontend corrections only within the user's authorized continuation scope, using the published revision and SYSTEM deployment runbook. Confirm hosted behavior after deployment; Git publication alone is not deployment.

Preserve existing evidence rather than rerunning the deterministic test merely to reproduce proof. `results/dataflow_evaluation_20260920/frontend/REPORT.md` predates the dropdown, so its “no discovery API” finding is superseded locally. The historical `test_program/REPORT.md` missing-local-ORE statement is superseded only by remote inspection, not a local raw-data download.

## Remote access and retained evidence

Only the coordinating A role operates grp6 VNC/SSH or promotes runtime changes. The user handles competition-VM file transfers. Never restart Nexus during SmarTest. Re-read SYSTEM remote rules before machine work. The last session left the VM bound/idle, SMT8 Ready and Edge Available; bounded collectors ended. Reverify current state.

Browser gateway: `https://guasg.sandbox.gemini.te-cloud.advantest.com/tevmip-180-3-13-67-endtevmip/`. Rediscover browser SSH/VNC tabs; IDs are session-dependent. Verify hostname `group-6` and named grp6 dashboard rows (last observed host 180.3.13.36, Edge 180.3.13.209). Earlier group-3 tabs were not used for execution.

Fresh complete run: `b7deb9e5ff1641c6a16546eda090c121`. Raw VM directory: `/home/user/Case_Event/dataflow_evaluation_20260920_Ns5HDP`, retaining config backup, captures, audits, recipe/ORE copies and receiver logs. Use `fresh_complete.jsonl`; earlier `observed.jsonl` had an extraction newline issue. Local `fresh_observation.json` records browser-transcribed observations, not a raw download. The combined report includes SHA256 values for the runtime model, JSONL, ORE and EDL.

EDL: `/tmp/STDF/datalog_TestCase1.TestCase1_4site_ft_lot1_4site_finaltest_group-6_20260920004549.edl`, 42,738,914 bytes. Earlier isolated host replay: `/home/user/Case_Event/replay_investigation_20260920_MV75AR`, retained exited container `grp6-replay-investigation-20260920`.

VPS SSH: `root@103.27.132.88`. Hosted D1 path: `/opt/grp6-preview/shared/state/v3/d1/miniflare-D1DatabaseObject/faaf2b0445ab934c3aac48ddf0cdfade8f9bac050be98993748742cdd2cb05fb.sqlite`; open read-only for audits. Keep `.dev.vars` credentials private. Deployment helper: `deploy/vps-preview/publish.ps1`; consult SYSTEM and use a published revision.
