# Preserve exact evidence/code bytes so recorded SHA256s survive Git checkout.
# This rule is limited to this workstream; shared attributes are unchanged.
* -text whitespace=blank-at-eol,blank-at-eof,space-before-tab,cr-at-eol
