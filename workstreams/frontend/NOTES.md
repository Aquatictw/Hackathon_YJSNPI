# E — R3-20260919

## Progress
- COMPLETE — implementation and required local/browser acceptance passed. [R3-20260919][E] COMPLETE. Base `7f99026b23a33a356c2470caa184eef9176f3034`; own worktree `/private/tmp/grp6-e-r3`; local branch `teammate-e-frontend-r3` (R2 branch still in use). Initial status clean; startup `git fetch origin` and `git merge-base --is-ancestor 7f99026b23a33a356c2470caa184eef9176f3034 origin/main` passed. Earlier worktrees preserved.
- Implementation `1719ad4955c8e237c0df91577e4d2336f69428a8`: common accessible light/dark/system selector, semantic colors/contrast, truthful AI availability/retry, readable prediction precision with exact values retained, source-driven replay evaluation and filter/selection recovery. Six new presentation/contrast regressions; R2 lifecycle unchanged.

## Decisions
- Existing next-themes provider uses class, system default and `rtdi-theme` storage. Mounted selector avoids server/client mismatch; initial theme script handles appearance before hydration. Native selector is keyboard accessible; no new dependency or configuration.
- No fake AI answer or backend success. Dashboard disables AI pending config/unavailable backend/key and offers retry, replay or explicitly fixed-rule sandbox. Command UI stays read-only. Source/units/receipt limitations, exact raw values and investigation IDs/citations are preserved.
- W25 is never special-cased: evaluation text uses `expected` and `expected_first_device`. Accepted seed still reports W25 missed. Filtering moves selection into visible results and recovers from a zero-result filter.
- Inventory: `rg --files workstreams/frontend` lists only this NOTES.md, referenced by SYSTEM/team brief. Authored files: app/{layout.tsx,page.tsx,globals.css,dashboard.css,replay/page.tsx,replay/replay.css,sandbox/page.tsx}, components/theme-controls.tsx, lib/rtdi/ui-presentation.ts, tests/ui-presentation.test.mjs, plus this note. Layout imports AppTheme; all three routes import ThemeSelector; dashboard/replay consume ui-presentation; new tests import that helper/read global tokens. Existing six UI primitives and use-mobile.ts retained; replay/sandbox keep their primitive imports. No files deleted. Accepted R2 history remains `ba89a991b6ca612917ef87ba0f54c5c3723d7867:workstreams/frontend/NOTES.md`.

## Verification and feature matrix
Actual Codex in-app browser on localhost:5173, 1440×1000 desktop and 390×844 mobile. Public https://hackathon.aquatictw.com/ was inspected read-only: confirmed internal “載入 D 的資料” wording and unconfigured AI before replacing local wording. Browser screenshots/AX/DOM observations are retained in this task’s tool outputs; the following matrix is the durable text evidence. Viewport override reset afterward.

| Feature | Observed / tested result |
| --- | --- |
| Themes across /, /replay, /sandbox | Switched light/dark, navigated and reloaded; explicit choices persisted. Default and returning to system resolved dark under the current OS preference. Native Space → Down → Return selected light with visible focus. No simulated OS setting change; actual OS light-mode change was not tested. |
| Responsive surfaces | Desktop/mobile screenshots inspected for theme controls, cards, evidence, tables and unavailable states. DOM width checks returned scrollWidth=1440/390 respectively: no page overflow. Table/evidence inner scrolling retained. SVG series use theme colors; mobile labels enlarged. Text pairs ≥4.5:1; input/focus/chart pairs ≥3:1 checked by regression tests. Native selector popup checked; no custom dialogs added. |
| Dashboard data and scope | Local real D1 seed: 14 incidents/evidence, 114 events, 24 stage/site prediction rows and actuals. Selected W1/Site1 matched context, chart and source. Prediction shown as 29.1973 / 29.196; full 29.197315625834765 available in exact-value detail/title. Units unknown, receipt unavailable, replay/freshness labels remain visible. ArrowRight/Home/End move tabs/focus. |
| Reconnect / failed lookup / outage | Disconnect retained evidence; reload reconnected. Missing run displayed 404 guidance and cleared old evidence. Stopped only E's local dev process: SSE showed reconnect, retry config showed setting-read failure, saved evidence stayed, AI remained disabled. Restart/reload recovered SSE. All 11 R2 deferred-response lifecycle regressions pass. |
| Replay | W25 showed 0 alerts and source evaluation “未偵測到預期類型”; limitations retain missed W25. Filtering W25 to alerts selected visible W1. Keyboard navigated analysis/model/limits tabs; six-stage model table and all source limits inspected. Light/dark site curves, reference line and raw-evidence disclosure retained. Filter recovery and future changed source result are regression-tested. File import flow unchanged; file chooser/import not re-exercised. |
| AI / commands / sandbox | Dashboard unknown/unconfigured AI cannot send; retry and sandbox link visible. Sandbox openai mode disables composer without a key; demo mode returned labeled fixed-rule answer with evidence reference. Dark/light demo chart, raw units and no-command state inspected. Dashboard command empty state says read-only/no commands. All seven statuses and persistence/citations remain covered by existing automated tests; no real OpenAI call, new persisted investigation or real machine command was performed. |
| Hydration | Browser console inspection before intentional outage returned no warn/error entries; no hydration warning observed on navigation/reload. Expected network errors during intentional outage do not imply hydration defects. |

Commands from frontend unless marked root:
- `npm ci --offline --ignore-scripts --cache /private/tmp/grp6-e-npm-cache`: exit 0; `/private/tmp/grp6-e-r3-install.log`.
- `node scripts/local-backend.mjs migrate`: initial exit 1 (sandbox loopback/log-path permissions); `/private/tmp/grp6-e-r3-migrate.log`. Retried local-only with `WRANGLER_LOG_PATH=/private/tmp/grp6-e-r3-wrangler WRANGLER_SEND_METRICS=false`, escalated loopback permission: exit 0; `/private/tmp/grp6-e-r3-migrate-retry.log`.
- `INGEST_TOKEN=local-e-r3-seed npm run dev -- --host 127.0.0.1`: initial sandbox listener denied (`/private/tmp/grp6-e-r3-dev.log`). Escalated start worked but Worker did not inherit token, so first local seed rejected with `ingest_not_configured` (task output). Resolved with minimal `env -i PATH="$PATH" HOME="$HOME" TMPDIR=/private/tmp CLOUDFLARE_INCLUDE_PROCESS_ENV=true INGEST_TOKEN=local-e-r3-seed npm run dev -- --host 127.0.0.1`; no .dev.vars/config edits. Logs `/private/tmp/grp6-e-r3-dev-seeded.log` and `/private/tmp/grp6-e-r3-dev-recovered.log`.
- Seed ran inline Node equivalent of local-backend.mjs seed, using existing replayJsonlToEdgeBatches / predictionSeedBatches / validateSeedAck, reading accepted results/replay inputs and posting solely to localhost with disposable local token. This avoided writing forbidden .dev.vars. Exit 0; `/private/tmp/grp6-e-r3-seed.log`.
- Root `node --test frontend/tests/ui-presentation.test.mjs`: initial five cases passed; `/private/tmp/grp6-e-r3-presentation.log`. Added filter recovery sixth case before final full test.
- `npm test`: 90/90 passed, exit 0; `/private/tmp/grp6-e-r3-test.log`.
- `npx tsc --noEmit`: exit 0; `/private/tmp/grp6-e-r3-tsc.log` (earlier initial check also exit 0).
- `npm run build`: exit 0; `/private/tmp/grp6-e-r3-build.log`. Existing vinext route-classification warning remains.
- Root `git diff --check`, staged check, explicit changed/untracked allowlist audit: passed. Implementation commit contains only the ten UI/test paths listed above; this notes file is the only additional authored path.

- Publication sync: final `git fetch origin` succeeded; merged `fca8912da03db0f00db94ff990e59f4d32bade4a` with `git merge --no-edit origin/main` without conflicts, merge `8e7c66911f9a40f3b82f3656fe679d67b3feb49a`. Incoming A/B/C research/docs and shell LF attribute preserve rejected-candidate evidence; no frontend code/dependency changes. `git diff --exit-code 1719ad4 HEAD -- frontend` exited 0, so passing checks/browser observations were reused under minimal synchronization. All E authored commits and staged/untracked paths audited separately from imported owner files. Local preview process stopped after checks.

- First normal push was rejected (`fetch first`) because A published `2afe662` during delivery. Followed the authorized race protocol: fetched the delta and merged without conflicts. Only SYSTEM and VPS deployment documentation/verification changed; frontend remains byte-identical to tested implementation (`git diff --exit-code 1719ad4 HEAD -- frontend`, exit 0). No frontend rerun needed. Final notes-only commit records this retry; no force push.

## Blockers
- None for local implementation. No outside-scope edit requested. Real paid AI, auth, live Edge transport, units/deadlines and correlated machine receipts remain unverified acceptance owned by A; no VPS/VM/deployment changes made.

## Handoff
- Implementation `1719ad4955c8e237c0df91577e4d2336f69428a8`; browser handoff `e3163b3`. Final completion/delivery SHA supplied in response after explicit `git push origin HEAD:refs/heads/main`. E deliverables/checks are recorded above; no implementation blocker. Next owner A: combined integration review and deployment decision. Stop editing after confirmed publication; no claim of deployment/live acceptance.
